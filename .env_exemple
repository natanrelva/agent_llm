# Exemplo de arquivo .env para configurar o agente Node.js com Express

# Host do servidor Ollama (geralmente localhost:11434)
OLLAMA_HOST=http://localhost:11434

# Modelo de Large Language Model (LLM) a ser usado para o raciocínio do agente.
# Certifique-se de que este modelo foi baixado via 'ollama pull <nome_do_modelo>'.
LLM_MODEL=llama3

# Modelo de Embedding a ser usado para o RAG (Geração Aumentada por Recuperação).
# Certifique-se de que este modelo foi baixado via 'ollama pull <nome_do_modelo>'.
EMBEDDING_MODEL=nomic-embed-text
